name: Build Editor Guide PDF

on:
  push:
    branches: ["main"]
    paths:
      - "EDITOR_GUIDE.typ"
      - ".github/workflows/build-guide.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-pdf:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Typst
        uses: typst-community/setup-typst@v3
        with:
          typst-version: 'latest'

      - name: Build PDF
        run: typst compile EDITOR_GUIDE.typ EDITOR_GUIDE.pdf

      - name: Check if PDF changed
        id: check_changes
        run: |
          if git diff --quiet EDITOR_GUIDE.pdf 2>/dev/null; then
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            echo "changed=true" >> $GITHUB_OUTPUT
          fi

      - name: Commit PDF
        if: steps.check_changes.outputs.changed == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add EDITOR_GUIDE.pdf
          git commit -m "docs: Update compiled EDITOR_GUIDE.pdf [skip ci]"
          git push
